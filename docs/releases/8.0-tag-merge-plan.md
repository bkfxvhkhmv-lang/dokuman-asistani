# BriefPilot 8.0 — Tag & Merge Plan
**Hazırlayan:** 2026-06-05
**HEAD:** `6f56ac1e2`
**Branch:** `feature/ocr-api-integration`

---

## Adımlar (sırayla)

### 1. Tag oluştur
```bash
git tag -a v8.0.0 -m "BriefPilot 8.0 — OCR v4 + Reply Assistant P0"
```

### 2. Push tag
```bash
git push origin v8.0.0
```

### 3. Branch'i main/master'a merge et
```bash
# main branch'e geç
git checkout main

# Merge (fast-forward yerine merge commit ile — geçmiş net kalsın)
git merge --no-ff feature/ocr-api-integration -m "chore(release): merge BriefPilot 8.0 (feature/ocr-api-integration)"

git push origin main
```

### 4. Eski branch'i koru (sil değil)
Branch'i şimdilik silme. 8.1 çalışması için referans olarak kalabilir.

---

## TestFlight

Merge sonrası EAS Build veya Xcode Archive ile TestFlight build tetikle:
```bash
eas build --platform ios --profile preview
```
ya da Xcode → Product → Archive → Distribute App → TestFlight

---

## 8.1 Branch

8.1 çalışması için merge sonrası:
```bash
git checkout -b feature/8.1-p1-polish main
```

---

## Checklist

- [ ] Tag oluştur: `v8.0.0`
- [ ] Tag push
- [ ] `feature/ocr-api-integration` → `main` merge
- [ ] `main` push
- [ ] TestFlight build tetikle
- [ ] `feature/8.1-p1-polish` branch aç
