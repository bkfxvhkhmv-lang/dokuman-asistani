/** Sabit bildirim planlayıcı kimliği (günlük özet iptali için). */
export const DAILY_DIGEST_IDENTIFIER = 'briefpilot_daily_digest';

/** Android 8+: yerel bildirimler için kanal (FCM/`remoteMessageChannelId` ile aynı isim kullanılmalı). */
export const ANDROID_DEFAULT_NOTIFICATION_CHANNEL_ID = 'briefpilot_default';
